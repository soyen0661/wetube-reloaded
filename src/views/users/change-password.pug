extends ../base

block content 
    if(errorMessage)
        span.error-message=errorMessage
    form(method="POST")
        input(name="oldPassword",placeholder="Current Password",type="password",required)
        input(name="newPassword",placeholder="New Password",type="password",required)
        input(name="confirmPassword",placeholder="New Password Confirmation",type="password",required)
        input(type="submit",value="Change Password")