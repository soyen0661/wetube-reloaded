extends ../base

block content
    h4 Change Title of video
    form(method="POST")
        input(name="title", placeholder="Video Title",type="text", value=video.title, maxlength=60,required)
        input(name="description", placeholder="Description", type="text", value=video.description, minlength=20,required)
        input(name="hashtags", placeholder="Hashtags, separated by comma.", type="text", value=video.hashtags.join(),required)
        input(value="Save", type="submit")
    