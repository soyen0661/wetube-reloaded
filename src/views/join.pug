extends base

block content
    if errorMessage 
        span.error-message=errorMessage
    form(method="POST")
        input(name="name",placeholder="Name",required,type="text")
        input(name="email",placeholder="E-mail",required,type="email")
        input(name="username",placeholder="Username",required,type="text")
        input(name="password",placeholder="Password",required,type="password")
        input(name="password2",placeholder="Confirm Password",required,type="password")
        input(name="location",placeholder="Location",type="text")
        input(value="Join",type="submit")
        include partials/social-login
    div.auth-switch
        span Already have an account? 
        a(href="/login") Log in &rarr;